# FoxClean Spec Alias

The original automation prompt referenced `sped.md`, but this workspace only
contained `spec.md` when implementation started. `spec.md` is the actual
OpenSpec source used for the FoxClean implementation and traceability audit.

Use `spec.md` as the source of truth.
