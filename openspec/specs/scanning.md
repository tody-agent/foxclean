# Scanning Spec

FoxClean SHALL discover installed apps, protected apps, junk files, large files, installers, and orphaned files. Path matching MUST use multiple confidence levels and cancellation-aware async scanning.
