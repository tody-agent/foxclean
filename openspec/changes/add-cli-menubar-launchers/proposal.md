# Proposal: CLI Polish

Implemented a package executable named `fox` with interactive no-arg TUI,
command routing, JSON output, completions, GUI open URL support, and Touch ID
status/dry-run/sudo-backed enable/disable commands.
