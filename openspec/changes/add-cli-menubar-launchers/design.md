# Design: CLI, Menu Bar, and Launchers

The `fox` CLI offers direct commands for scans, status, analysis, cleanup,
toolkit flows, shell completion, and app opening. Menu bar actions mirror the
highest-frequency GUI actions. Raycast scripts call the same CLI commands so
launcher behavior stays aligned with terminal behavior.
