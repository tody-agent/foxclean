# Proposal: Disk Analyzer + System Monitor

Implemented in `DiskScanner`, squarified `TreemapLayout`, `AnalyzerView`,
`SystemMonitor`, `HealthScore`, the AppKit menu bar quick-action widget, and
CLI `analyze`/`status`.
